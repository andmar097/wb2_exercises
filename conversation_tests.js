"use strict"

//the variables we are going to work with
let a = "101.1 ";
let b = "55";
let c = "402 Stevens"
let d = "Number 5  ";

//using parseInt for each variable and displaying the results

console.log("parseInt: " + parseInt(a) + ", " + parseInt(b) + ", " + parseInt(c) + ", " + parseInt(d));

//using parseFloat for each variable and displaying the results

console.log("parseFloat: " + parseFloat(a) + ", " + parseFloat(b) + ", " + parseFloat(c) + ", " + parseFloat(d));

//using Number for each variable and displaying the results

console.log("Number: " + Number(a) + ", " + Number(b) + ", " + Number(c) + ", " + Number(d));

//using unary for each variable and displaying the results

console.log("Unary operator (+): " + +a + ", " + +b + ", " + +c + ", " + +d);